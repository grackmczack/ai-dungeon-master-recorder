# Frontend environment variables (optional — API is proxied via nginx)
# VITE_API_BASE=/api

# Optional analytics. Leave both empty until the consent-gated web and server
# containers are published. Container IDs are public configuration, not secrets.
VITE_GTM_CONTAINER_ID=
VITE_GTM_SERVER_URL=https://analytics.dnd-recorder.de
